###############
# DOCKER ENV  #
###############

# Database Configuration (PostgreSQL in Docker)
# These values are used by docker-compose.yml
DB_NAME=hikvision_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432

# Django Settings
SECRET_KEY=your-secret-key-change-this-in-production
DEBUG=True

# Use "*" to allow access from LAN (e.g. 192.168.1.129)
# You can replace "*" with specific IPs/hosts separated by ";"
ALLOWED_HOSTS=*

